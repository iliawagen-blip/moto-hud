{
  "name": "moto-hud",
  "version": "1.0.0",
  "private": true,
  "description": "Мото ИЛС — HUD-навигатор для мотоцикла",
  "scripts": {
    "dev": "node scripts/build-web.mjs && npx --yes serve -l 3456 -c serve.json .",
    "spike:routers": "node scripts/spike-routers.mjs",
    "build": "node scripts/build-web.mjs",
    "nav:replay": "node scripts/nav-replay.mjs --baseline fixtures/baseline_v0.json",
    "cap:sync": "npm run build && npx cap sync",
    "cap:android": "npm run cap:sync && npx cap open android"
  },
  "dependencies": {
    "@capacitor-community/background-geolocation": "^1.2.26",
    "@capacitor-community/keep-awake": "^6.0.0",
    "@capacitor-community/text-to-speech": "^6.1.0",
    "@capacitor/android": "^7.2.0",
    "@capacitor/app": "^7.0.1",
    "@capacitor/core": "^7.2.0",
    "@capacitor/geolocation": "^7.1.2",
    "@capacitor/local-notifications": "^7.0.7",
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.2.0",
    "esbuild": "^0.28.1"
  }
}
